import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Checkout | OlympiadPDFs', robots: { index: false } };
export default function CheckoutPage() {
  return <div className="container-site section" style={{textAlign:'center',color:'var(--color-neutral-500)'}}>Checkout — coming in Phase 2.</div>;
}
